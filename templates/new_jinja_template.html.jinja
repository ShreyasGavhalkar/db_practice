<html>
<head>
  <style>
    h1 {text-align:center;}
    table {
  font-family: arial, sans-serif;
  font-size: 25px;
  width: 50%;
}
  td {background-color: #dddddd;}
  .center {margin-left: auto; margin-right: auto;}
  </style>
</head>
<body>
  <h1>
  <b>Total number of tables are: {{tables|length}}</b>
  </h1>
<h2>{%- if tables|length == 0 %}
      No tables present
    {%- else %}
      They are listed below:
    {%- endif %}
  </h2>
  <table class="center">
    {%- for item in tables %}
    <tr>
      <td>
        <a href="{{item}}.html">
        {{item}}
        </a>
      </td>
    </tr>
    {% endfor %}
  </table>
</body>
</html>
